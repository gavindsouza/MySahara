<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sfit.sahara.mysahara">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- test -->


        <!--
            intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter
        -->
        <activity
            android:name=".junior_fifth_page_home"
            android:label="@string/title_activity_normal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".first_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".senior_first_page" />
        <activity android:name=".junior_third_page_details" />
        <activity android:name=".junior_second_page_login" />
        <activity android:name=".junior_second_page_signup" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA2hxE7Q2CunOikmFtiHBxTLOxuXpYHx4s" />

        <activity
            android:name=".junior_add_location"
            android:label="@string/title_activity_junior_add_location" />
        <activity android:name=".senior_second_page" />
        <activity android:name=".junior_second_page_after_login_signup"></activity>
    </application>

</manifest>